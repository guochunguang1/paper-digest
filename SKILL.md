---
name: paper-digest
description: 单篇学术 PDF → 自包含中文解读 HTML：题目与摘要中英对照、研究背景与科学问题、方法要点、重点总结、逐图视觉解读（渲染裁图后看图说话）、局限性与批判性评价。触发词：文献解读、读文献、帮我读这篇文章、PDF 总结、论文总结、文献总结、摘要翻译、逐图解读、figure 解读、paper digest、文献阅读报告、解读这篇文献、这篇文章讲了什么。
---

# paper-digest — 单篇 PDF → 中文深度解读 HTML

## 定位与边界

**一句话**：给一篇学术 PDF，产出一个可分享的单文件 HTML 深度解读（题目/摘要中英对照 + 背景 + 方法 + 重点总结 + 逐图看图解读 + 局限性批判）。

**负责**（端到端 5 步）：PDF 确定性提取（文本 + 逐图裁图）→ 全文精读 → 逐图视觉解读 → 组装自包含 HTML → 内容质检。

**不负责**：
- 批量多篇：外层循环逐篇调用本 skill，每篇独立输出目录
- 全文翻译：只译题目与摘要，全文翻译请用通用翻译工具
- 扫描版 OCR：无文本层 PDF 直接告警并停（→ 先用 OCR 工具处理）
- 数据重分析、统计结果复核：只解读不重算
- 引文真实性 / 科研诚信核查：→ `fakefetch`
- 补充材料单独 PDF：默认不处理；用户显式要求时对第二份 PDF 手动 `--pages` 模式

**前置依赖**：python3 + PyMuPDF（缺失时安装：`python3 -m pip install --user PyMuPDF`，见 errorbook）。平台需具备读文本、读图（vision）、写文件能力。

## 参数契约（默认值 + 松弛级联）

| 参数 | 默认值 | 何时改 / 怎么改 |
|------|--------|----------------|
| 目标语言 | 中文 | 用户明确要求其他语言时改 |
| 图范围 | 主图 Figure 1–N | 用户要看补充图 → 脚本 `--include-supp`；图注检测失效 → `--pages 4,6` 手动指定页 |
| 详略程度 | 标准档（重点总结 300–500 字 + 每图一段解读） | 用户说"精读"→ 深档（每图多段 + 统计方法点评）；"速览"→ 简档（每节 1–2 句） |
| 输出目录 | `<PDF 同目录>/<PDF 名>_digest/` | 用户指定位置 → `--outdir` |
| 渲染倍率 | 2.2（≈158dpi） | 图字太小看不清 → 升到 3.0 重裁 |

**必问用户**：无（PDF 路径即唯一必需输入；其余全部有默认值）。**可推断**：文献语言（自动检测，中英均可——英文文献译成中文，中文文献题目摘要保留原文）、期刊信息（从文本首页提取 DOI/期刊/年份，提不到就留空不编造）。

## 默认执行路径

**Step 0 前置检查**
- 意图：避免中途崩。输入：PDF 路径。动作：确认文件存在；`python3 -c "import fitz"` 验证依赖，缺则装。
- 检查点：依赖就绪。

**Step 1 确定性提取（脚本）**
- 意图：把 PDF 变成 LLM 可靠消费的中间产物，理解与提取分离。
- 输入：PDF。动作：`python3 templates/script-skeleton.py --pdf <PDF路径>`。
- 检查点：`extract/paper_text.md` 非空且多页；`extract/manifest.json` 中 figures 数量与图注一致；脚本报告"疑似扫描版"则停，向用户说明。

**Step 2 全文精读（LLM）**
- 意图：读透文章。动作：读 `paper_text.md`，确定题目（英文原文 + 中文译）、摘要（原文 + 译文）、研究背景与缺口、方法要点、核心发现、局限性。摘要/题目识别以内容语义为准，不依赖字面 "Abstract" 标题（部分期刊无此标题）。
- 检查点：七块内容全部有着落，无编造（提不准的回读原文对应页）。

**Step 3 逐图视觉解读（LLM 看图）**
- 意图：看图说话，不只复述图注。动作：逐张 Read `extract/figures/fig*.png` + 对照 manifest 里的原图注 + 正文引用该图的结果段落，写解读（这张图做了什么 / 怎么做的 / 结果说明什么）。裁剪明显不对（缺面板/含无关正文）→ 用 `--pages <该图页码>` 重裁后重看。
- 检查点：每张图都有解读且与图片实际内容一致（引用图中真实的分组、数值趋势）。

**Step 4 组装 HTML**
- 意图：单文件自包含可分享。动作：按 `templates/report-template.html` 版式，图片 base64 内嵌，占位符全部替换为真实内容。
- 检查点：写盘为 `<PDF 名>_digest.html`。

**Step 5 质检门（内容断言，不是"文件存在即成功"）**
- 意图：防假完成（参见全局 PITFALLS：HTML 报告存在 ≠ 内容完整）。动作：断言 ① 七个章节标题全部在场；② 无 `{{占位符}}` 残留；③ HTML 内嵌图数 == manifest 图数；④ base64 图可解码（`data:image/png;base64` 计数一致）；⑤ 题目/摘要中英双语都在场。任一不过 → 修复重新生成，不放水。
- 检查点：全部断言通过后，向用户报告产物路径 + 解读要点摘要。

## 输出契约

输入 PDF 只读。产出目录（默认 `<PDF 同目录>/<PDF 名>_digest/`）：

```
<PDF 名>_digest/
  <PDF 名>_digest.html      # 最终交付：单文件自包含解读报告
  extract/
    paper_text.md           # 逐页标记的全文文本（中间产物）
    manifest.json           # 图注清单：编号/页码/坐标/裁剪框/图注文本
    figures/fig01.png ...   # 逐图裁剪 PNG（中间产物）
```

## 失败恢复表

| 症状 | 常见原因 | 恢复动作 |
|------|----------|----------|
| 脚本报"疑似扫描版" | PDF 无文本层（扫描件） | 停，告知用户需先 OCR；不硬解读 |
| 图注定位 0 张或漏图 | 图表编号样式特殊（如 "Fig. 1—"、图内嵌图注） | 人工从 `paper_text.md` 找图所在页 → `--pages` 手动模式重裁 |
| 裁剪图不对（缺面板/混入正文） | 图注在图上方（非主流版式）或跨栏图 | `--pages <页码>` 整页裁剪兜底；`--zoom` 调清晰度 |
| 跨页大图只裁到一半 | 图跨两页 | 两页各裁一次，HTML 中上下拼接展示 |
| `import fitz` 失败 | 未装 PyMuPDF | `python3 -m pip install --user PyMuPDF`；新环境坑记 errorbook |
| PDF 加密报错 | 文档有密码保护 | 停，请用户提供解密版 |
| HTML 质检断言不过 | 生成时章节/图缺失 | 按断言信息补齐重新生成，禁止降质放行 |

## 能力映射（跨平台降级）

| 能力 | 全量路径 | 降级路径 |
|------|----------|----------|
| 读图 vision | 逐图看图解读 | 无 vision 平台：基于图注 + 正文引用文字归纳解读，并在报告 footer 声明"未看图，基于文字归纳" |
| 执行脚本 | 本机跑提取脚本 | 无执行环境（prompt-only）：产出脚本 + 运行指令交用户本地跑，诚实声明未验证提取结果 |
| 结构化提问 | 点选参数 | 编号列表点选 |

## subagent 衔接

- **fan-out 单元**：单张图的视觉解读（一个图 = 一个单元），图多时可并行后按编号合并；逐图解读结果须由主 agent 统一语气终审。
- **编排模式**：单线为主；tournament / loop-until-done 无适用场景。

## 执行后回看

质检门通过后：新踩的非显然局部坑（版式怪例、裁剪边界情况）追加到本 skill `errorbook.md`；跨 skill / 跨工具的坑（PyMuPDF 平台差异等）登记全局 `PITFALLS.md`（判据见 sogen-skill-dev `references/pitfalls-policy.md`）。
